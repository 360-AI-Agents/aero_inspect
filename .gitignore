# Python virtual environment -- never commit this
venv/

# Python bytecode/cache
__pycache__/
*.pyc
*.pyo

# Editor/OS cruft
.vscode/
.DS_Store
Thumbs.db

# Environment secrets -- never commit
.env

# Uploaded content -- real photos/videos of workers, never commit these
# Evidence photos and model weights are intentionally NOT ignored.

# Live HLS stream output -- transient files regenerated during live detection
stream/

# Node / Wrangler
node_modules/
.wrangler/
package-lock.json

# Local sqlite artifact from running the backend locally
aeroinspect.db
